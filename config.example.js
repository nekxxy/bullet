// Copy this file to `config.js` and fill in your Supabase project values.
// `config.js` is gitignored — never commit your keys.
// If `config.js` is missing, the game falls back to localStorage-only scores.
window.SUPABASE_URL = '';
window.SUPABASE_ANON_KEY = '';
